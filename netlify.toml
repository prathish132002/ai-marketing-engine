# Netlify configuration for monorepo — points to the frontend sub-folder
[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
